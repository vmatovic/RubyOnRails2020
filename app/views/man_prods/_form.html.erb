<%= form_with(model: man_prod) do |form| %>
  <% if man_prod.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(man_prod.errors.count, "error") %> prohibited this man_prod from being saved:</h2>

      <ul>
        <% man_prod.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :manufacturer_id %>
    <%= form.text_field :manufacturer_id %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
